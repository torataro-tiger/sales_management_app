import { Typography } from '@mui/material';
import { Routes, Route, Link } from 'react-router-dom';

const BasicInfo = () => {
  return (
    <h1>BasicInfo Page</h1>
  );
};

export default BasicInfo;
