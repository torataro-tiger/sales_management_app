import { Typography } from '@mui/material';
import { Routes, Route, Link } from 'react-router-dom';

const MonthSummary = () => {
  return (
    <h1>MonthSummary Page</h1>
  );
};

export default MonthSummary;
